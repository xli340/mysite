query Post($postId: Int!) {
  post(id: $postId) {
    id
    title
    text
    points
    voteStatus
    creatorId
    creator {
      id
      createdAt
      updatedAt
      username
      email
    }
    createdAt
    updatedAt
    textSnippet
  }
}